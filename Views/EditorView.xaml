<UserControl x:Class="MusicGame.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="#FFFFFF">
    
    <Grid>
        <!-- 标题栏 -->
        <Border HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20" Background="#F0F0F0" Padding="20" CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="谱面编辑器" FontSize="24" FontWeight="Bold" Foreground="#000000" />
            </StackPanel>
        </Border>
        
        <!-- 编辑器区域 -->
        <Grid Margin="20,80,20,120" Background="#FFFFFF">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            
            <!-- 左侧编辑区域 -->
            <Grid Grid.Column="0">
                <!-- 六线（垂直线，等间距分布） -->
                <Canvas x:Name="StringsCanvas" Background="Transparent">
                    <!-- 垂直线，间距150 -->
                    <Line X1="100" Y1="0" X2="100" Y2="600" Style="{StaticResource StringLineStyle}" />
                    <Line X1="250" Y1="0" X2="250" Y2="600" Style="{StaticResource StringLineStyle}" />
                    <Line X1="400" Y1="0" X2="400" Y2="600" Style="{StaticResource StringLineStyle}" />
                    <Line X1="550" Y1="0" X2="550" Y2="600" Style="{StaticResource StringLineStyle}" />
                    <Line X1="700" Y1="0" X2="700" Y2="600" Style="{StaticResource StringLineStyle}" />
                    <Line X1="850" Y1="0" X2="850" Y2="600" Style="{StaticResource StringLineStyle}" />
                </Canvas>
                
                <!-- 音符容器 -->
                <Canvas x:Name="NotesCanvas" Background="Transparent" />
                
                <!-- 时间轴 -->
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden" Margin="0,580,0,0">
                    <Canvas x:Name="TimelineCanvas" Height="40" Background="#F0F0F0" />
                </ScrollViewer>
            </Grid>
            
            <!-- 右侧属性面板 -->
            <Grid Grid.Column="1" Margin="10,0,0,0" Background="#F0F0F0">
                <StackPanel>
                    <TextBlock Text="音符属性" FontSize="16" FontWeight="Bold" Foreground="#000000" Margin="10,10,10,5" />
                    
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Column="0" Grid.Row="0" Text="轨道:" FontSize="14" Foreground="#000000" VerticalAlignment="Center" />
                        <ComboBox Grid.Column="1" Grid.Row="0" x:Name="LineComboBox" Width="100" Margin="5" />
                        
                        <TextBlock Grid.Column="0" Grid.Row="1" Text="时间:" FontSize="14" Foreground="#000000" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Grid.Row="1" x:Name="TimeTextBox" Width="100" Margin="5" />
                        
                        <TextBlock Grid.Column="0" Grid.Row="2" Text="时长:" FontSize="14" Foreground="#000000" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Grid.Row="2" x:Name="DurationTextBox" Width="100" Margin="5" />
                        
                        <StackPanel Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="5">
                            <Button Content="添加音符" Style="{StaticResource GameButton}" Width="80" Click="AddNoteButton_Click" />
                            <Button Content="删除" Style="{StaticResource GameButton}" Width="80" Click="DeleteNoteButton_Click" />
                        </StackPanel>
                    </Grid>
                    
                    <Separator Margin="10" Height="1" Background="#CCCCCC" />
                    
                    <TextBlock Text="音乐" FontSize="16" FontWeight="Bold" Foreground="#000000" Margin="10,10,10,5" />
                    <StackPanel Margin="10">
                        <Button Content="加载音乐" Style="{StaticResource GameButton}" Click="LoadMusicButton_Click" />
                        <Button Content="播放音乐" Style="{StaticResource GameButton}" Click="PlayMusicButton_Click" />
                        <Button Content="暂停音乐" Style="{StaticResource GameButton}" Click="PauseMusicButton_Click" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
        
        <!-- 底部控制栏 -->
        <Border HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20" Background="#F0F0F0" Padding="20" CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <Button Content="保存谱面" Style="{StaticResource GameButton}" Click="SaveScoreButton_Click" />
                <Button Content="加载谱面" Style="{StaticResource GameButton}" Click="LoadScoreButton_Click" />
                <Button Content="清空所有" Style="{StaticResource GameButton}" Click="ClearAllButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
<UserControl x:Class="MusicGame.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="#1A1A1A">
    
    <Grid>
        <!-- 标题栏 -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20" Background="#2D2D2D" Padding="20">
            <TextBlock Text="Score Editor" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" />
        </StackPanel>
        
        <!-- 编辑器区域 -->
        <Grid Margin="20,80,20,120" Background="#2D2D2D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            
            <!-- 左侧编辑区域 -->
            <Grid Grid.Column="0">
                <!-- 六线 -->
                <Canvas x:Name="StringsCanvas" Background="Transparent">
                    <Line X1="0" Y1="50" X2="1000" Y2="50" Style="{StaticResource StringLineStyle}" />
                    <Line X1="0" Y1="150" X2="1000" Y2="150" Style="{StaticResource StringLineStyle}" />
                    <Line X1="0" Y1="250" X2="1000" Y2="250" Style="{StaticResource StringLineStyle}" />
                    <Line X1="0" Y1="350" X2="1000" Y2="350" Style="{StaticResource StringLineStyle}" />
                    <Line X1="0" Y1="450" X2="1000" Y2="450" Style="{StaticResource StringLineStyle}" />
                    <Line X1="0" Y1="550" X2="1000" Y2="550" Style="{StaticResource StringLineStyle}" />
                </Canvas>
                
                <!-- 音符容器 -->
                <Canvas x:Name="NotesCanvas" Background="Transparent" />
                
                <!-- 时间轴 -->
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden" Margin="0,580,0,0">
                    <Canvas x:Name="TimelineCanvas" Height="40" Background="#1A1A1A" />
                </ScrollViewer>
            </Grid>
            
            <!-- 右侧属性面板 -->
            <Grid Grid.Column="1" Margin="10,0,0,0" Background="#1A1A1A">
                <StackPanel>
                    <TextBlock Text="Note Properties" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="10,10,10,5" />
                    
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="30" />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Column="0" Grid.Row="0" Text="Line:" FontSize="14" Foreground="#FFFFFF" VerticalAlignment="Center" />
                        <ComboBox Grid.Column="1" Grid.Row="0" x:Name="LineComboBox" Width="100" Margin="5" />
                        
                        <TextBlock Grid.Column="0" Grid.Row="1" Text="Time:" FontSize="14" Foreground="#FFFFFF" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Grid.Row="1" x:Name="TimeTextBox" Width="100" Margin="5" />
                        
                        <TextBlock Grid.Column="0" Grid.Row="2" Text="Duration:" FontSize="14" Foreground="#FFFFFF" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Grid.Row="2" x:Name="DurationTextBox" Width="100" Margin="5" />
                        
                        <StackPanel Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="5">
                            <Button Content="Add Note" Style="{StaticResource GameButton}" Width="80" Click="AddNoteButton_Click" />
                            <Button Content="Delete" Style="{StaticResource GameButton}" Width="80" Click="DeleteNoteButton_Click" />
                        </StackPanel>
                    </Grid>
                    
                    <Separator Margin="10" Height="1" Background="#444444" />
                    
                    <TextBlock Text="Music" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="10,10,10,5" />
                    <StackPanel Margin="10">
                        <Button Content="Load Music" Style="{StaticResource GameButton}" Click="LoadMusicButton_Click" />
                        <Button Content="Play Music" Style="{StaticResource GameButton}" Click="PlayMusicButton_Click" />
                        <Button Content="Pause Music" Style="{StaticResource GameButton}" Click="PauseMusicButton_Click" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
        
        <!-- 底部控制栏 -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20" Background="#2D2D2D" Padding="20" CornerRadius="5">
            <Button Content="Save Score" Style="{StaticResource GameButton}" Click="SaveScoreButton_Click" />
            <Button Content="Load Score" Style="{StaticResource GameButton}" Click="LoadScoreButton_Click" />
            <Button Content="Clear All" Style="{StaticResource GameButton}" Click="ClearAllButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
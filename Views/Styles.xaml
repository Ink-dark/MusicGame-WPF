<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 全局样式定义 -->
    
    <!-- 按钮样式 -->
    <Style x:Key="GameButton" TargetType="Button">
        <Setter Property="Background" Value="#4CAF50" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Margin" Value="5" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#45a049" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#3e8e41" />
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- 音符样式 -->
    <Style x:Key="NoteStyle" TargetType="Ellipse">
        <Setter Property="Fill" Value="#FF5722" />
        <Setter Property="Stroke" Value="#FFFFFF" />
        <Setter Property="StrokeThickness" Value="2" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="5" Color="#000000" Opacity="0.5" />
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- 判定线样式 -->
    <Style x:Key="JudgmentLineStyle" TargetType="Line">
        <Setter Property="Stroke" Value="#FFEB3B" />
        <Setter Property="StrokeThickness" Value="3" />
        <Setter Property="StrokeDashArray" Value="5,5" />
    </Style>
    
    <!-- 六线样式 -->
    <Style x:Key="StringLineStyle" TargetType="Line">
        <Setter Property="Stroke" Value="#FFFFFF" />
        <Setter Property="StrokeThickness" Value="2" />
        <Setter Property="Opacity" Value="0.8" />
    </Style>
</ResourceDictionary>
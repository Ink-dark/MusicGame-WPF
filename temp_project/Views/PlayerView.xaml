<UserControl x:Class="MusicGame.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="#1A1A1A">
    
    <Grid>
        <!-- 游戏标题 -->
        <TextBlock Text="MusicGame" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" VerticalAlignment="Top" 
                   Margin="10" Foreground="#FFFFFF" />
        
        <!-- 游戏区域 -->
        <Grid x:Name="GameArea" Margin="20,60,20,120" Background="#2D2D2D">
            <!-- 六线 -->
            <Canvas x:Name="StringsCanvas" Background="Transparent">
                <Line X1="0" Y1="50" X2="1000" Y2="50" Style="{StaticResource StringLineStyle}" />
                <Line X1="0" Y1="150" X2="1000" Y2="150" Style="{StaticResource StringLineStyle}" />
                <Line X1="0" Y1="250" X2="1000" Y2="250" Style="{StaticResource StringLineStyle}" />
                <Line X1="0" Y1="350" X2="1000" Y2="350" Style="{StaticResource StringLineStyle}" />
                <Line X1="0" Y1="450" X2="1000" Y2="450" Style="{StaticResource StringLineStyle}" />
                <Line X1="0" Y1="550" X2="1000" Y2="550" Style="{StaticResource StringLineStyle}" />
            </Canvas>
            
            <!-- 音符容器 -->
            <Canvas x:Name="NotesCanvas" Background="Transparent" />
            
            <!-- 判定线 -->
            <Line x:Name="JudgmentLine" X1="0" Y1="300" X2="1000" Y2="300" Style="{StaticResource JudgmentLineStyle}" />
        </Grid>
        
        <!-- 游戏信息 -->
        <Grid Margin="20,680,20,20" Height="60" Background="#2D2D2D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                <TextBlock Text="Score" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
                <TextBlock x:Name="ScoreText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#FFEB3B" />
            </StackPanel>
            
            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="Combo" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
                <TextBlock x:Name="ComboText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" />
            </StackPanel>
            
            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                <TextBlock Text="Accuracy" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
                <TextBlock x:Name="AccuracyText" Text="0%" FontSize="24" FontWeight="Bold" Foreground="#2196F3" />
            </StackPanel>
        </Grid>
        
        <!-- 控制按钮 -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20" Orientation="Horizontal">
            <Button Content="Play" Style="{StaticResource GameButton}" Click="PlayButton_Click" />
            <Button Content="Pause" Style="{StaticResource GameButton}" Click="PauseButton_Click" />
            <Button Content="Stop" Style="{StaticResource GameButton}" Click="StopButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>